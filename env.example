# Responsibility:
# - Provide a minimal, truthful template for required environment variables.
#
# Notes:
# - Copy this file to `.env.local` (recommended) or `.env`.
# - `DATABASE_URL` defaults to `file:./prisma/dev.db` if not provided (see `lib/prisma.ts`).

# Option A (simple): Groq API key via env
# - If you prefer, you can set the API key from the Settings page instead (Option B).
GROQ_API_KEY=

# Option B (recommended for local): master key to encrypt secrets stored in DB (AES-256-GCM)
# - Generate: `npm run key:gen`
# - Required if you want to save the Groq API key from the Settings page.
APP_CONFIG_ENCRYPTION_KEY=

# Optional: Model id used by the Mastra router / chat agent fallback.
# Example: groq/llama-3.3-70b-versatile
# For LMSTUDIO: lmstudio/model-name (e.g., lmstudio/lfm2-8b-a1b)
# Backward compatible: GROQ_MODEL is still supported (deprecated).
MODEL_ID=groq/llama-3.3-70b-versatile
GROQ_MODEL=

# Optional: LMSTUDIO configuration (OpenAI-compatible local server)
# Base URL for LMSTUDIO server (e.g., http://127.0.0.1:1234)
LMSTUDIO_BASE_URL=http://127.0.0.1:1234
# API key for LMSTUDIO (optional, defaults to "lm-studio" if not set)
LMSTUDIO_API_KEY=lm-studio

# Optional: SQLite database location for Settings (Prisma).
# Example: file:./prisma/dev.db
DATABASE_URL="file:./prisma/dev.db"

